# `@t-med/prettier-config`

> T-MED [Prettier](https://prettier.io) config.

## Usage

**Install**:

```bash
$ yarn add --dev @t-med/prettier-config
```

**Edit `package.json`**:

```jsonc
{
  // ...
  "prettier": "@t-med/prettier-config"
}
```
